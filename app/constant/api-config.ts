/* App config for apis
 */
const domain = "";
// const domain = "http://localhost:3000";

const baseUrl = domain + "/app/";

const ApiConfig = {
	domain,
	baseUrl,
	sendOtp: "login",
	
};

export default ApiConfig;
